@* Generator: Template *@
@functions { public dynamic Model { get; set; } }@{
    var moduleDash = Model.Module == null ? "" : (Model.Module + "-");
}

.s-@(moduleDash)@(Model.ClassName)Dialog {
    > .size { .widthAndMin(650px); }
    .dialog-styles(@@h: auto, @@l: 150px, @@e: 400px);
    .s-PropertyGrid .categories { height: 260px; }
}