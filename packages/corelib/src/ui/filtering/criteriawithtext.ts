
/**
 * A criteria expression paired with its human-readable display text.
 */
export interface CriteriaWithText {
    /** The criteria expression. */
    criteria?: any[];
    /** The display text describing the criteria. */
    displayText?: string;
}
